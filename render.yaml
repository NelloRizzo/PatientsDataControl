services:
  - type: web
    name: patientshealthbridge-api
    runtime: node
    region: ohio
    plan: free
    branch: main
    buildCommand: |
      NODE_ENV=development npm install && \
      npm run build --workspace=packages/shared && \
      npm run build --workspace=packages/backend
    startCommand: node packages/backend/dist/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
      - key: APP_URL
        value: https://patientshealthbridge-app.onrender.com
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: EMAIL_HOST
        value: smtp-relay.brevo.com
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_SECURE
        value: "false"
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: EMAIL_FROM
        value: noreply@healthbridge.com

  - type: web
    name: patientshealthbridge-app
    runtime: node
    region: ohio
    plan: free
    branch: main
    buildCommand: |
      NODE_ENV=development npm install && \
      npm run build --workspace=packages/shared && \
      npm run build --workspace=packages/frontend
    startCommand: node packages/frontend/server.js
    envVars:
      - key: VITE_API_URL
        value: https://patientshealthbridge-api.onrender.com
